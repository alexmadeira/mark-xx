import { TScrollCreateProps } from '@/services/controller/scroll'

export const defaultScrollProps = {
  duration: 3,
  syncTouch: true,
  autoRaf: true,
  autoResize: true,
} satisfies TScrollCreateProps

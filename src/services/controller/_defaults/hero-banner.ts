import type { THeroBannerProps } from '@/services/controller/hero-banner-controller'

export const defaultHeroBannerProps = {
  start: 'web',
  delay: 3000,
  speed: 120,
  deletionSpeed: 60,
} satisfies THeroBannerProps
